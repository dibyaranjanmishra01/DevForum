import React from 'react';
import './thread.css';
import Button from '@mui/material/Button';

export default function Thread() {
  return (
    <div className='threadContainer'>
      Thread
      <Button variant="contained" id="routeButton">Route Page</Button>
    </div>
  )
}
